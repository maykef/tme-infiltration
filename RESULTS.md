# RESULTS — TME spatial-graph infiltration predictor

_Auto-generated by `src/make_results.py`. Shows the original 100-image run and the corrected full-dataset run side by side (see `docs/CORRECTIONS.md`)._

## 0. Correction summary

The original Stage 1a pull used `JacksonFischer_2020_BreastCancer(data_type="sce")` with the **undocumented default `full_dataset=FALSE`**, which silently returns only the ~100-image **Basel** subset. The corrected run uses `full_dataset=TRUE` (Basel + **Zurich** cohorts). See `docs/CORRECTIONS.md` and `docs/marker_overlap.md`.

## 1. Datasets used (after filtering)

| | Original (Basel subset) | Corrected (full: Basel+Zurich) |
|---|---|---|
| Jackson pretrain slides | 100 | **723** |
| Jackson cells | 285,851 | **1,240,267** |
| Jackson markers used | 33 | **36** |
| Moldoveanu slides (eval) | 30 | 30 |

- **Moldoveanu (unchanged):** 30 slides = 30 patients, **14 responders / 16 non-responders**, 35 markers, 112,497 cells. Evaluation = leave-one-patient-out.
- **Marker panel overlap (Jackson∩Moldoveanu): only 8 shared proteins, 12.7% Jaccard** (`docs/marker_overlap.md`) — low semantic overlap, a structural reason to expect weak cross-panel transfer regardless of pretraining corpus size.

## 2. Headline — GNN vs. Giuliani baseline (both runs)

| Model | Original (Basel, 100 img) AUROC [95% CI] | Corrected (full) AUROC [95% CI] |
|---|---|---|
| **Spatial-graph GNN** (pretrained + fine-tune) | 0.460 [0.253, 0.679] | **0.576 [0.360, 0.789]** |
| **Giuliani baseline** (cross-PCF @10.5 µm) | 0.833 [0.660, 0.964] | **0.833 [0.660, 0.964]** |

**Corrected verdict: GNN does NOT beat baseline.** Even with ~6× the pretraining corpus, the GNN does **not** beat the hand-crafted spatial statistic; the GNN CI still crosses 0.5 and overlaps the baseline.

## 3. Ablations with bootstrap CIs (both runs)

2000× patient-resample bootstrap on every configuration (not just the headline).

| GNN configuration | Original AUROC [95% CI] | Corrected AUROC [95% CI] |
|---|---|---|
| Pretrained + fine-tune (headline) | 0.460 [0.253, 0.679] | 0.576 [0.360, 0.789] |
| Pretrained trunk **frozen** | 0.406 [0.205, 0.625] | 0.567 [0.348, 0.796] |
| **No pretraining** (random init) | 0.545 [0.330, 0.760] | 0.545 [0.330, 0.760] |

**All three corrected-run CIs mutually overlap** — the pretrained / frozen / no-pretrain ordering is **not statistically distinguishable**; it is within bootstrap noise. No claim that pretraining helps or hurts is supported.

## 4. Wall-clock (RTX PRO 6000 Blackwell)

- Pretraining (full Jackson, 723 graphs, early-stop @ epoch 93): **347.7 s**
- Fine-tune + LOPO eval (30 folds + baseline + 2000× bootstrap): **320.5 s**
- Counterfactual gradient search: **13.2 s**

## 5. Counterfactual findings — GATED

> ⚠️ **These are NOT biological findings.** The counterfactual gradients were computed on a model whose corrected discrimination (§2) does **not** clear the baseline and whose CI crosses chance. They are recorded only as a mechanistic demonstration that the gradient search runs; they must not be interpreted as biology unless/until a model that beats the baseline is obtained.

<details><summary>Mechanistic output (do not cite as findings)</summary>

1. In non-responder slide 22RD (model P(response)=0.001), the most influential cells are Tc.ae cells, and the counterfactual gradient most often points to 'CCR7 (increase)' (6/25 of the top cells) as the marker shift that would most raise predicted response.
2. In non-responder slide 31RD (model P(response)=0.0), the most influential cells are Th.ae cells, and the counterfactual gradient most often points to 'CCR7 (increase)' (9/25 of the top cells) as the marker shift that would most raise predicted response.
3. In non-responder slide 35RD (model P(response)=0.001), the most influential cells are melano cells, and the counterfactual gradient most often points to 'CCR7 (increase)' (21/25 of the top cells) as the marker shift that would most raise predicted response.
4. In non-responder slide 01RD (model P(response)=0.0), the most influential cells are macro.mono cells, and the counterfactual gradient most often points to 'CCR7 (increase)' (24/25 of the top cells) as the marker shift that would most raise predicted response.
5. In non-responder slide 19BL (model P(response)=0.001), the most influential cells are melano cells, and the counterfactual gradient most often points to 'CCR7 (increase)' (23/25 of the top cells) as the marker shift that would most raise predicted response.

</details>

## 6. Reproduce

```bash
bash scripts/build_env.sh   # once
bash run_all.sh            # Stages 1..6 -> this file
```
All scripts take `--seed` (default 42); runs logged to `results/run_log.jsonl`. Contract: `docs/data_schema.md`; corrections: `docs/CORRECTIONS.md`; panel overlap: `docs/marker_overlap.md`.
